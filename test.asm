EXR 12
ADD R7, R8
ADD R0, 5

ADC R1, R0;
SUB R2, R0;
SBB R3, R0;
OR  R4, R0;
AND R5, R0;
XOR R6, R7;

; MOV
; MOV
; MOV
; MOV
; MOV
; MOV
; MOV
; JR
; CP
; ADD
; INC
; DEC
; DSZ
; OR
; AND
; XOR
; EXR
; BIT
; BSET
; BCLR
; BTG
; RRC
; RET
; SKIP
; RX,RY
; RX,RY
; RX,RY
; RX,RY
; RX,RY
; RX,RY
; RX,RY
; RX,RY
; RX,#N
; [XY],R0
; R0,[XY]
; [NN],R0
; R0,[NN]
; PC,NN
; NN
; R0,N
; R0,N
; RY
; RY
; RY
; R0,N
; R0,N
; R0,N
; N
; RG,M
; RG,M
; RG,M
; RG,M
; RY
; R0,N
; F,M